# Photonix 环境配置（简化示例）

# 服务监听端口
PORT=13001
NODE_ENV=production
# 日志级别（error|warn|info|debug）
LOG_LEVEL=info

PHOTOS_DIR=/app/photos
DATA_DIR=/app/data

# === Redis（缓存/限流/队列） ===
# 生产（Compose 内）常用：redis://redis:6379
# 本机开发常用：redis://localhost:6379
REDIS_URL=redis://redis:6379

# Redis 启用开关（可选）
# 设为 false 时，系统将使用内存缓存替代 Redis
# 默认：true（推荐生产环境使用 Redis）
ENABLE_REDIS=true

# === 安全（必须修改为安全值） ===
# JWT 签名密钥：请替换为 32+ 位强随机字符串
JWT_SECRET=CHANGE_ME_TO_A_SECURE_32_PLUS_CHAR_STRING
# 管理操作口令：请替换为强口令
ADMIN_SECRET=admin

