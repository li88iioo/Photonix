# Photonix 环境配置（简化示例）
# 用途：复制为 .env 并按需修改；仅包含核心必配项，适合快速上线
# 部署：Docker Compose 使用 env_file: .env 注入；本机/PM2 请以进程环境注入

# === 核心服务 ===
# 服务监听端口
PORT=13001
# 运行模式（生产环境请设为 production）
NODE_ENV=production
# 日志级别（error|warn|info|debug）
LOG_LEVEL=info

# === 目录（容器内路径，需挂载持久化） ===
# 媒体库根目录（图片/视频）
PHOTOS_DIR=/app/photos
# 数据目录（数据库/缩略图/HLS 等）
DATA_DIR=/app/data

# === Redis（缓存/限流/队列） ===
# 生产（Compose 内）常用：redis://redis:6379
# 本机开发常用：redis://localhost:6379
REDIS_URL=redis://redis:6379

# === 安全（必须修改为安全值） ===
# JWT 签名密钥：请替换为 32+ 位强随机字符串
JWT_SECRET=CHANGE_ME_TO_A_SECURE_32_PLUS_CHAR_STRING
# 管理操作口令：请替换为强口令
ADMIN_SECRET=CHANGE_ME_TO_STRONG_ADMIN_PASSWORD